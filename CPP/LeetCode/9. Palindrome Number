class Solution {
public:
    bool isPalindrome(int x) {
        //if(x<0){return false;}
        string s = to_string(x);
        string c=s;
        reverse(s.begin(),s.end());
        if(s == c){
            return true;
        }
        else{
            return false;
        }
    }
};
